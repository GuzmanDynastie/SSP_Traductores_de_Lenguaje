Este es un ejemplo de texto donde se utilizan diferentes caracteres y se separan lineas con el caracter ';'. Cada vez que se encuentra un ';' en el texto, el programa añade una nueva linea para mantener el formato adecuado.

En este ejemplo, la expresion regular [^;\n]+ coincide con cualquier secuencia de caracteres que no contenga ';' ni saltos de linea. Cuando se encuentra una coincidencia con esta expresion regular, el programa imprime el texto correspondiente utilizando printf("%s", yytext), lo que resulta en la impresion de texto normal.

Ademas, la expresión regular ";" coincide con el caracter ';'. Cuando se encuentra un ';', el programa imprime una nueva linea utilizando printf("\n"), lo que resulta en la impresion de una nueva linea despues de cada ';' encontrado.

Finalmente, la expresion regular .|\\n coincide con cualquier otro caracter que no sea ';'. Esto asegura que cualquier otro caracter, incluyendo saltos de linea, se imprima normalmente utilizando printf("%s", yytext).

Este texto de ejemplo demuestra como el programa procesa el texto de entrada segun las reglas definidas por las expresiones regulares, manteniendo el formato y manipulando adecuadamente los caracteres segun sea necesario.